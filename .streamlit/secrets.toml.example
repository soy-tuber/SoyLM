# Copy to .streamlit/secrets.toml locally, or paste into the
# Streamlit Cloud "Secrets" panel for deployment.
# Both keys are optional — users can also paste keys in the UI at runtime.

# anthropic_api_key = "sk-ant-..."
# google_api_key    = "AIza..."
